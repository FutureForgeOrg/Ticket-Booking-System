export interface Movie {
  id: string;
  title: string;
  year: number;
  genres: string[];
  director: string;
  actors: string;
  plot: string;
  runtime: string;
  posterUrl: string;
  bannerUrl: string;
  trailerUrl: string;
}
